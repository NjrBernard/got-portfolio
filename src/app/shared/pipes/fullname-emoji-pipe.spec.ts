import { FullnameEmojiPipe } from './fullname-emoji-pipe';

describe('FullnameEmojiPipe', () => {
  it('create an instance', () => {
    const pipe = new FullnameEmojiPipe();
    expect(pipe).toBeTruthy();
  });
});
